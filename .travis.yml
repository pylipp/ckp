dist: xenial
language: python
python:
    - 3.5
    - 3.6
    - 3.7

install:
    - pip install .
    - pip install coveralls
    - cp ./database.kdbx ~/.database.kdbx

script:
    - coverage run --source . test.py
    - ckp --help

after_success:
    - coveralls
